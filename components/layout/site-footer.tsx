export function SiteFooter() {
  return (
    <footer className="border-t border-border bg-background/92">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-6 py-6 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between">
        <p>Dining Management frontend scaffold for Firebase-authenticated workflows.</p>
        <p>Dhaka business date rules and role-based flows are enforced in the app layer.</p>
      </div>
    </footer>
  );
}
